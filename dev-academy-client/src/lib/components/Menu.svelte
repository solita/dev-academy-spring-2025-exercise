

<!-- <script lang="ts">
	import { evacLogo, getIcon } from '$lib/assets';
	import { menuButton } from '$lib/stores';

	// TODO: Display app info
	//import { getName, getVersion } from '@tauri-apps/api/app';
	//let appInfo = Promise.all([getName(), getVersion()]);
</script> -->

<!-- <header class="menu">
	{#if $menuButton}
		<button type="button" class="menuButton" on:click={$menuButton.clickHandler}>
			<!-- eslint-disable-next-line svelte/no-at-html-tags -->
			<!-- {@html getIcon($menuButton.icon)}
		</button>
	{/if}
	<div class="bottom">
		<!-- eslint-disable-next-line svelte/no-at-html-tags -->
		<!--{@html evacLogo}
	</div>
</header> -->

<!-- <style>
	header {
		--menu-width: 8rem;

		display: flex;
		flex-direction: column;
		justify-content: start;
		align-items: center;
		height: 100%;
		width: var(--menu-width);
		min-width: var(--menu-width);
		padding: var(--spacing-default);

		background-color: var(--color-ocean-depth);
	}

	.menuButton {
		width: 100%;
		aspect-ratio: 1;
		padding: 20%;

		border-radius: 20px;
		border: none;
		background-color: transparent;
		color: var(--color-white);
	}

	.menuButton:active {
		background-color: var(--color-pistachio);
	}

	.bottom {
		--logo-width: calc(var(--menu-width) * 1.8);
		position: absolute;
		bottom: 0;
		transform-origin: center;
		transform: rotate(-90deg) translateX(calc(var(--logo-width) / 2));
		width: var(--logo-width);
	}
</style> -->
